#include <bits/stdc++.h>

using namespace std;
struct node{
    int data;
    node* next;
};

int main(){
    int m, n; scanf(" % d % d", &n, &m);
    node* head;
    while ((n--) > 1) {
        for ()
    }
    return 0;
}